# ============================================================
#  AI-Studio (设计院) — 环境变量配置模板
#  复制此文件为 .env 并修改对应值
# ============================================================

# ── 数据存储 ──
# 相对路径会按项目根目录解析
STUDIO_DATA_PATH=./dev-data
WORKSPACE_PATH=./
# 是否强制以 WORKSPACE_PATH 作为活跃工作目录（覆盖数据库中已保存的当前目录）
# 1=true 启用；0=false 关闭（默认关闭）
WORKSPACE_PATH_FORCE=0

# ── 认证 ──
STUDIO_ADMIN_USER=admin
STUDIO_ADMIN_PASS=admin123
STUDIO_SECRET_KEY=change-me-to-a-random-string

# Token 有效天数
STUDIO_TOKEN_EXPIRE_DAYS=7

# ── GitHub 集成 (可选) ──
# GitHub Personal Access Token (用于 Models API 和仓库操作)
GITHUB_TOKEN=
# GitHub 仓库 (格式: owner/repo)
GITHUB_REPO=

# Git 平台 (github | gitlab)，默认 github
GIT_PROVIDER=github

# GitLab 配置 (当 GIT_PROVIDER=gitlab 时使用)
GITLAB_URL=https://gitlab.com
# GitLab Token (PAT / Project Access Token)
GITLAB_TOKEN=
# GitLab 仓库 (格式: namespace/project)
GITLAB_REPO=

# ── Git 仓库 (可选, 支持 GitLab/Gitea 等) ──
# GIT_CLONE_URL=https://gitlab.example.com/org/repo.git

# ── SVN 仓库 (可选, 当 VCS_TYPE=svn 或 auto 检测到 .svn 时生效) ──
# VCS_TYPE=auto          # auto | git | svn  — 默认 auto 自动检测
# SVN_REPO_URL=https://svn.example.com/repo
# SVN_USERNAME=
# SVN_PASSWORD=
# SVN_TRUNK_PATH=trunk   # svn info 中 trunk 相对路径

# ── 主项目 SSO (可选) ──
# MAIN_API_URL=http://your-main-app/api
# SSO_TOKEN_KEY=token

# ── 部署配置 (可选) ──
# DEPLOY_SERVICES=frontend,backend
# DEPLOY_GIT_BRANCH=master
# DEPLOY_HEALTH_CHECKS=backend=http://backend:8000/api/health;frontend=http://frontend:80
# DOCKER_IMAGE_PREFIX=myproject

# ── 本地部署脚本配置 (deploy.sh / deploy.bat) ──
# 后端监听地址
DEPLOY_BACKEND_HOST=0.0.0.0
DEPLOY_BACKEND_PORT=8002

# 前端预览监听地址 (vite preview)
DEPLOY_FRONTEND_HOST=0.0.0.0
DEPLOY_FRONTEND_PORT=4174

# Linux tmux 会话名（仅 deploy.sh --tmux 使用）
DEPLOY_TMUX_SESSION=ai-studio-deploy
